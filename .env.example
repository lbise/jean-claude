# Optional override for where Jean-Claude stores auth state.
# Default: ~/.jean-claude
# JEAN_CLAUDE_STATE_DIR=/absolute/path/to/state

# Optional override for where Jean-Claude looks for prompts.
# Default: ./prompts or the bundled prompts directory
# JEAN_CLAUDE_PROMPTS_DIR=/absolute/path/to/prompts
