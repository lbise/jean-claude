version: 1
mode: recommend
base_layers:
  - base/jeanclaude.md
  - base/policies.md
mode_prompt: modes/recommend.md
skills_catalog: skills/catalog.yaml
initial_state: rank

states:
  rank:
    task_context: |
      Rank candidates against the profile.
      Return concise rationale and confidence.
    output_schema: schemas/recs.json
    transitions:
      - to: rank
